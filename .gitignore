
# Build and caches
/build/
/dist/
/nuitka_dist/
/__pycache__/
/final/__pycache__/
/.pytest_cache/
/pytest-cache-files-*/
*.pyc

# Generated files
/EncomendasEdy.spec
/run_app.spec
/build_log.txt
/nuitka-crash-report.xml
/final/app/_version.py

# Segredos — nunca commitar
.env
# Config central com credenciais — vive no Gist secreto, não no repo.
# Fica local só como fallback embutido no build.
lojas.config
.admin_token
# Localização do gist central (a URL do gist é a credencial de leitura).
remote.json

# Ambientes virtuais
.venv/
.venv312/

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
